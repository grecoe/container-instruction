apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "forreview.fullname" . }}-test-connection"
  labels:
    {{- include "forreview.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "forreview.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
